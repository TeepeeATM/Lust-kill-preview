"use client";
import { useState } from "react";
import { useBag } from "./bag-provider";
export function AddToBag({slug,name,sizes,colorways}:{slug:string;name:string;sizes:string[];colorways:string[]}){const [size,setSize]=useState(sizes[0]);const [color,setColor]=useState(colorways[0]);const [added,setAdded]=useState(false);const {add}=useBag();return <div className="purchase-panel">{colorways.length>1&&<fieldset><legend>SELECT COLOR</legend><div className="size-grid">{colorways.map(value=><button className={color===value?"selected":""} type="button" key={value} onClick={()=>setColor(value)} aria-pressed={color===value}>{value}</button>)}</div></fieldset>}<fieldset><legend>SELECT SIZE</legend><div className="size-grid">{sizes.map(value=><button className={size===value?"selected":""} type="button" key={value} onClick={()=>setSize(value)} aria-pressed={size===value}>{value}</button>)}</div></fieldset><button className="primary-button" type="button" onClick={()=>{add({slug,name,size,color});setAdded(true)}}>{added?"ADDED TO BAG ✓":"ADD TO BAG"}</button><p className="microcopy">Secure checkout accepts major cards and supported digital wallets after the client connects their payment accounts.</p></div>}
